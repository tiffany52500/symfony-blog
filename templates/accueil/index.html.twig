{% extends 'base.html.twig' %}

{% block title %}Accueil{% endblock %}

{% block body %}
    <div class="rounded-4 border border-3 text-center py-2 mb-3">
        <p class="fw-bold m-0 p-0"><em>Bienvenue sur notre blog ! Nous publions des articles tous les jours !</em></p>
    </div>

    <ol class="list-group mb-3">
            <div class="list-group-item d-flex justify-content-center align-items-start">
                <strong class="text-primary">NOS ARTICLES LES PLUS RÉCENTS</strong>
            </div>
        {% for article in articles %}
            <a class="list-group-item d-flex justify-content-between align-items-start" href="{{ path("app_article_slug", {'slug': article.slug}) }}">
                <div class="ms-2 me-auto">
                    {{ article.titre }}
                </div>
                <span class="badge bg-primary rounded-pill">{{ article.createdAt | date('d/m/Y') }}</span>
            </a>
        {% endfor %}
    </ol>
{% endblock %}
